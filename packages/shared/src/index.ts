/**
 * @satcom/shared - Shared Package
 *
 * This package contains shared code used across the Satcom workforce monorepo:
 * - Type definitions (enums, models, API contracts)
 * - Theme tokens (colors, typography, spacing, motion)
 * - Utility functions
 *
 * Usage:
 *   import { UserRole, WorkMode } from '@satcom/shared';
 *   import { colors, typography } from '@satcom/shared/theme';
 */

// Export all types
export * from './types';

// Export theme (also available via @satcom/shared/theme)
export * from './theme';
